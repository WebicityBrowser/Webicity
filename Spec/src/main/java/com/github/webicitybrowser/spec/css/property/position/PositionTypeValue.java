package com.github.webicitybrowser.spec.css.property.position;

import com.github.webicitybrowser.spec.css.property.CSSValue;

public enum PositionTypeValue implements CSSValue {
	STATIC, RELATIVE, ABSOLUTE, STICKY, FIXED
}
