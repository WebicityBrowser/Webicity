package com.github.webicitybrowser.spec.css.property.flexbox;

import com.github.webicitybrowser.spec.css.property.CSSValue;

public record FlexBasisValue(CSSValue size, boolean isAuto) implements CSSValue {
	
}
