/**
 * This package contains the various tokenize states
 * used by the HTML parser. The tokenizer is based on
 * the WHATWG HTML specification. The parser switches between
 * various states, each of which contains references to other
 * states that it may switch to.
 */
package com.github.webicitybrowser.spiderhtml.tokenize;
