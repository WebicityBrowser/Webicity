/**
 * This package contains the core classes for the drawing system.
 * Backend implementations can extend the interfaces in this package.
 * When possible, any drawing operations by higher-level modules should
 * be done through the interfaces in this package (unless a higher-level
 * abstraction is to be used).
 */
package com.github.webicitybrowser.thready.drawing.core;