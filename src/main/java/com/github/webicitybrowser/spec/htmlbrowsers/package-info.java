/**
 * This package contains implementations of parts of the
 * HTML specification that don't seem to have so much to
 * do with the actual parsing of HTML.
 */
package com.github.webicitybrowser.spec.htmlbrowsers;
