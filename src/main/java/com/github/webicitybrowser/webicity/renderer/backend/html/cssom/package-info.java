/**
 * Contains the CSS Object Model (CSSOM) implementation.
 * This is used to determine which CSS rules apply to
 * which elements, and what the computed style of each
 * element is. Note that this does not handle binding
 * CSS attributes and blocks to the component tree,
 * because that would depend on the frontend renderer.
 */
package com.github.webicitybrowser.webicity.renderer.backend.html.cssom;
