/**
 * This package contains parts of the rendering engine that
 * do not specifically relate to graphics rendering, specifications,
 * or major features. If you were to write another browser,
 * this would be one of the most important packages to use
 * (though you would also need to include dependencies such
 * as the specification and graphics system). It also contains
 * bindings between the rendering engine and the graphics system.
 */
package com.github.webicitybrowser.webicity;
